
<%= render 'shared/content_title', title: "User profiles" %>


<table class="table table-striped table-hover">
	<thead>
		<tr>
			<th>#</th>
			<th>Account</th>
			<th>Nickname</th>
			<th>Email</th>
			<th>Cellphone</th>
			<th>Groups</th>
		</tr>
	</thead>
	<tbody>
		<% @users.each_with_index do |user, index| %>
			<tr>
				<td><%= (index + 1) %></td>
				<td><%= link_to user.account, user_path(user) %></td>
				<td><%= user.nickname %></td>
				<td><%= user.email %></td>
				<td><%= user.cellphone %></td>
				<td><%= group_label(user.group_titles) %></td>
			</tr>
		<% end %>
	</tbody>
</table>

