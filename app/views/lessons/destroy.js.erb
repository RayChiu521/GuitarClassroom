<% if @lesson.errors.any? %>
	alert('<%= @lesson.errors.full_messages.to_sentence %>');
<% else %>
	$('#lessonContainer').hide();
	$('#calendarContainer').show();

	calendar.fullCalendar('removeEvents', <%= @lesson.id %>);
<% end %>